# appgsp

## Requisitos
- Node.js 20 (usa `nvm use` si tienes nvm).

## Arranque rapido
- `npm install`
- `npm run start:clear`

## Notas
- Si el proyecto esta en OneDrive, marca la carpeta como
  "Always Keep on This Device" para evitar errores `ETIMEDOUT`
  al leer archivos desde el disco.
